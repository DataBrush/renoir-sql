[package]
name = "sql-macro-test"
version = "0.1.0"
edition = "2024"

[dependencies]
renoir-sql = { path = "../../langs/renoir-sql" }
renoir = "0.5"
serde = { version = "1.0.228", features = ["derive"] }
rdkafka = "0.38.0"
